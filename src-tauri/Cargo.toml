[package]
name = "ai-manager-desktop"
version = "0.1.0"
description = "AI Manager desktop wrapper"
authors = ["AI Manager"]
license = ""
repository = ""
edition = "2021"
rust-version = "1.77"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["unstable"] }
tao = "0.35.2"
wry = "0.55.1"

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]

[package.metadata]
tauri-build = true
